/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["api.testvalley.kr", "dvd6ljcj7w3pj.cloudfront.net", "images.unsplash.com"],
    }
};

export default nextConfig;
